Future Improvements

I plan to add the following features in future versions:

Improve the overall UI design
Add dark/light mode toggle
Add a password visibility toggle
Ensure at least one selected character type is included in the generated password
Add password entropy/security score
Show a "Copied!" message instead of using alerts
Increase the maximum password length
Add smooth animations and transitions
Make the project fully responsive for mobile devices
